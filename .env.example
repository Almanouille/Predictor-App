# Environment Configuration
# Set to 'local', 'dev', or 'production'
ENV=local

# Football API Configuration
# Get your API key from https://www.api-football.com/
API_KEY=your_api_key_here

# Database Configuration (Optional)
# PostgreSQL connection string for remote storage
DATABASE_URL=postgresql://username:password@host:port/database

# Neon Database Configuration (Optional)
# For remote PostgreSQL storage using Neon
NEON_DATABASE_URL=postgresql://username:password@host:port/database

# Airflow Configuration (Optional)
# Only needed if using Airflow for orchestration
DOCKERFILE=Dockerfile
AIRFLOW__WEBSERVER__SECRET_KEY=your_secret_key_here
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__CORE__LOAD_EXAMPLES=False
AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow